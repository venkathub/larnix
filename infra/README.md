# infra

Operations & CI:

- GitHub Actions workflows (execute notebooks, lint prose, link-check, spell-check)
- docker-compose for local site preview
- deploy config (GitHub Pages / Netlify / Vercel)
