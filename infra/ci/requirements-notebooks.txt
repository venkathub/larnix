# R10 notebook-execution gate deps (infra/ci/run_notebooks.py).
# Pinned to the versions verified locally on the gate fixtures + the chapter twin.
nbclient==0.11.0
ipykernel==7.3.0

# Chapter-twin runtime deps (the M0 sample chapter's twin trains a scikit-learn
# classifier on Iris; M1/M3 data chapters load vendored CSVs via lib/data.py;
# M2/M3 chapters plot with Matplotlib). Pinned to versions verified executing the twins.
numpy==2.5.0
scikit-learn==1.9.0
pandas==3.0.4
matplotlib==3.11.0

# CPU-scaled colab-companion deps (P2-D9 / DECISIONS D0020): CI executes the
# generated companions with exact-pinned CPU-only torch wheels from the official
# CPU index (~200 MB-class vs ~800 MB+ CUDA; cached in Actions). Colab itself is
# NOT pinned — companions carry a version-floor guard instead (P2-D11).
# Pair verified together on python:3.12 with numpy 2.5.0 (backward + numpy
# interop smoke), 2026-07-19.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
torchvision==0.28.0+cpu

# M4 Ch12's twin runs real XGBoost under CPython, exact-pinned to the version
# Pyodide 0.28.1 ships in the browser (P2-D6 / DECISIONS D0020) — so the twin
# executes the same library version the learner runs.
xgboost==2.1.4

# M4 Ch18's twin draws dendrograms with scipy. Browser parity (Pyodide 0.28.1
# ships scipy 1.14.1) is NOT possible here: 1.14.1 predates numpy 2.5 and the
# pins conflict. Pinned instead to a current release verified against this
# file's numpy/sklearn pins (2026-07-21); the scipy.cluster.hierarchy API used
# (linkage/dendrogram/fcluster) is long-stable across both versions.
scipy==1.16.3
