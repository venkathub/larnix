StylesPath = styles
MinAlertLevel = error

# Learner-facing content is authored in Quarto (.qmd). The Larnix style enforces
# the STYLE_GUIDE §3 tone gate (banned words + no hype) on that content.
[*.qmd]
BasedOnStyles = Larnix
